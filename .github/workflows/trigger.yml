name: Trigger test

on:
  push:
    branches:
    - test

jobs:
  test_job:
    name: test_job
    runs-on: [ubuntu-latest]
    env: 
      foo: 'bar'
    steps:
      - name: test
        run: |
          echo env.foo
